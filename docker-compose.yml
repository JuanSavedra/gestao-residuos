version: '3.8'

services:
  app-springboot:
    build: .
    container_name: gestao-residuos-app
    ports:
      - "${SERVER_PORT:-8080}:8080"
    environment:
      - DB_URL=${DB_URL:-jdbc:oracle:thin:@oracle-db:1521:XE}
      - DB_USER=${DB_USER:-system}
      - DB_PASS=${DB_PASS:-oracle}
      - SPRING_JPA_HIBERNATE_DDL_AUTO=${SPRING_JPA_HIBERNATE_DDL_AUTO:-update}
      - SERVER_PORT=8080
    depends_on:
      - oracle-db
    networks:
      - backend-network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/api/actuator/health"]
      interval: 40s
      timeout: 20s
      retries: 5
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 768M
    restart: on-failure

  oracle-db:
    image: gvenzl/oracle-xe:latest
    container_name: oracle-db
    ports:
      - "1521:1521"
    environment:
      - ORACLE_PASSWORD=${ORACLE_PASSWORD:-oracle}
    volumes:
      - oracle-data:/opt/oracle/oradata
    networks:
      - backend-network
    healthcheck:
      test: ["CMD", "bash", "-c", "echo 'SELECT 1 FROM DUAL;' | sqlplus -L system/${ORACLE_PASSWORD:-oracle}@//localhost:1521/XE"]
      interval: 30s
      timeout: 20s
      retries: 15
      start_period: 90s
    deploy:
      resources:
        limits:
          memory: 2048M

volumes:
  oracle-data:

networks:
  backend-network:
    driver: bridge